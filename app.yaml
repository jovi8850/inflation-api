runtime: python311

entrypoint: gunicorn -w 2 -k uvicorn.workers.UvicornWorker app.main:app

service_account: inflation-api-sa@inflation-project-012926.iam.gserviceaccount.com

automatic_scaling:
  min_instances: 0
  max_instances: 1